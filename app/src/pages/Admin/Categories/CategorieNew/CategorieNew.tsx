import * as React from "react";
import { useQuery } from "react-query";
// import { fetchUsers } from "../../services/users";

export const CategorieNew = () => {
  // const { data, status } = useQuery("users", fetchUsers);

  return (
    <main>
      <h1>CategorieNew</h1>
    </main>
  );
};
